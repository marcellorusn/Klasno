# .yarn directory (placeholder)

This folder contains Yarn runtime artifacts in projects that use Yarn Berry (v2+) and PnP.
The real contents (releases, cache, etc.) are generated by running `yarn install`.

Keep this placeholder so the repository layout matches the course structure.
