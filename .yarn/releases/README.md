# .yarn/releases (placeholder)

This folder typically contains the Yarn release binary used by the project (e.g. `yarn-4.10.3.cjs`).
Run `yarn set version berry` or `yarn install` to generate the actual release file.
